

{% extends "base.twig" %}

{% block content %}
    
    <div class="container mx-auto page my-6">
         {% if not has_hero_in_acf %}<h1 class="text-6xl font-semibold mb-8">{{ post.title }}</h1> {% endif %}
        {{ post.content }}
    </div>

{% endblock %}
