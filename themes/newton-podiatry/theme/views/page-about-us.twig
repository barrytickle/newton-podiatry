{% extends "base.twig" %}

        {# {{dump(post.thumbnail.src)}} #}
{% block content %}
    <div class="container mx-auto my-6">
        <div class="flex flex-col md:flex-row">
            <div class="w-full md:w-1/2 md:pr-14">
                <img src="{{ post.thumbnail.src }}" alt="{{ post.thumbnail.alt }}" class="w-full h-full object-cover">
            </div>
            <div class="page-about-us page w-full md:w-1/2 md:pr-14 text-lg">
                <h1 class="text-6xl font-semibold mb-8">{{ post.title }}</h1>
                {{ post.content }}
            </div> 
        </div>
        
    </div>
{% endblock %}
