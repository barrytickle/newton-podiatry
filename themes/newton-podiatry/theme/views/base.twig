<!doctype html>
<html {{ site.language_attributes }}>
<head>
    <meta charset="{{ site.charset }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{site_header_title}}</title>
    <link rel="pingback" href="{{ site.pingback_url }}" />
    <link rel="stylesheet" href="{{site.theme_uri}}/theme/assets/dist/assets/main-B3Qf5L5T.css">
    <style>
        [x-cloak] { display: none; }
    </style>
    {{function('wp_head')}}
</head>

<body class="{{body_class}} text-primary overflow-x-hidden" data-template="base.twig">
    {% include 'header.twig' %}

    {% block content %}
    Sorry, no content
    {% endblock %}

    {% block footer %}
        {% include 'footer.twig' %}
        {{ function('wp_footer') }}
    {% endblock %}
</body>
</html>
